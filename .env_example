# ==============================================================================
#                         环境变量配置文件 (.env_example)
# ==============================================================================

# ----------------------------------------------------------------------------
# 1. 核心大语言模型 (LLM) 配置
#    用于 WikiDocu 的文档问答功能和 Podcast-LLM 的内容生成
# ----------------------------------------------------------------------------

# 硅基流动 SiliconFlow API 配置 (推荐用于主模型)
# 获取地址: https://www.siliconflow.cn/
OPENAI_API_KEY=your-siliconflow-api-key
OPENAI_MODEL=model-name
OPENAI_BASE_URL=base-url

# 魔搭 DashScope API 配置 (可选备用)
# 获取地址: https://dashscope.console.aliyun.com/
# DASHSCOPE_API_KEY=sk-xxxx
# OPENAI_MODEL=qwen-plus  # 或 qwen-turbo, qwen-max 等
# OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# ----------------------------------------------------------------------------
# 2. 播客生成专用 LLM 配置
#    Podcast-LLM 使用不同的模型来优化不同任务的性能和成本
# ----------------------------------------------------------------------------

# 快速响应 LLM (用于播客大纲生成、脚本撰写等)
# 推荐使用速度较快、成本较低的模型
FAST_LLM_BASE_URL=base-url

# 长文本处理 LLM (用于播客内容研究、文档分析等)
# 推荐使用上下文长度较长、理解能力更强的模型
LONGCONTEXT_LLM_BASE_URL=base-url

# ----------------------------------------------------------------------------
# 3. 文本转语音 (TTS) 服务配置
#    用于将播客脚本转换为音频文件
# ----------------------------------------------------------------------------

# Tavily 搜索 API (用于播客内容研究)
# 获取地址: https://tavily.com/
TAVILY_API_KEY=your-api-key

# Google Cloud TTS API (可选)
# GOOGLE_API_KEY=your-google-api-key

# ElevenLabs TTS API (可选)
# ELEVENLABS_API_KEY=your-elevenlabs-api-key

# ----------------------------------------------------------------------------
# 4. WikiDocu 问答系统配置
# ----------------------------------------------------------------------------

# WikiDocu 默认问答文档目录
WIKIDOCU_QA_DIR=.QADocs

# ============================================================================
# 配置说明:
# 1. 请将此文件复制并重命名为 .env，然后填写您的实际 API 密钥
# 2. 注释掉 (#) 不使用的配置项，取消注释以启用相应服务
# 3. 根据需要调整模型名称和 API 地址
# ============================================================================